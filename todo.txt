@todo list

1. register still doesnt work, hashing/bcrypt problems?

2. post photos doesnt work, "Field 'title' doesn't have a default value"

3. put photos doesnt work, only showing the photo with  "'select `photos`.* from `photos` where `photos`.`id` = ? limit ?'".

4. get specific album doesnt work "Error: photos, users is not defined on the model.".

5. post album doesnt work, "Field 'title' doesn't have a default value"

6. put photos doesnt work, only showing the photo with  "'select `albums`.* from `albums`'"

7. post add photo to album doesnt work.

something wrong with my database script, check mysql